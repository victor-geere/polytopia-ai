# 6-3 Final Polish & Vercel Deployment

## Prompt

Perform final performance pass (instancing audit, texture sizes, shadow quality tiers, adaptive DPR). Add a simple loading screen and error boundary. Ensure the production build is optimized. Document the exact steps to deploy to Vercel (GitHub integration + any environment variables). Confirm the live URL works on mobile browsers.

## Supplementary Information

- Run Lighthouse or Web Vitals checks focused on mobile.
- Prefer static export / SPA mode on Vercel.
- Add a short “How to deploy” section to the project README.
- Acceptance: Production deployment on Vercel loads quickly, runs stably on mid-range phones, and allows a complete game session.
